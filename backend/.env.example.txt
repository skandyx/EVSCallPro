# ===================================================
# Fichier d'exemple pour les variables d'environnement du Backend
# Copiez ce fichier en .env et remplissez les valeurs.
# ===================================================

# --- Base de Données ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=contact_center_user
DB_PASSWORD=votre_mot_de_passe_securise
DB_NAME=contact_center_db

# --- Serveur AGI ---
AGI_PORT=4573

# --- Mode de Connexion Téléphonie ---
# Mettre 'ASTERISK_AMI' pour la nouvelle architecture centralisée.
# Mettre 'YEASTAR_API' pour l'ancien mode de connexion directe.
PBX_CONNECTION_MODE=ASTERISK_AMI

# --- Connexion Asterisk Manager Interface (AMI) ---
# Doit correspondre à la configuration dans asterisk-configs/manager.d/evscallpro.conf
AMI_HOST=127.0.0.1
AMI_PORT=5038
AMI_USER=ami_user
AMI_SECRET=ami_password

# --- Sécurité JWT & Cookies ---
# Utilisez des chaînes de caractères longues et aléatoires pour ces secrets.
JWT_SECRET=votre_cle_secrete_pour_access_tokens_jwt
JWT_REFRESH_SECRET=une_autre_cle_secrete_tres_longue_pour_refresh_tokens
COOKIE_SECRET=un_secret_pour_signer_les_cookies

# --- WebSocket Client (utilisé par le frontend) ---
# L'URL que le client React utilisera pour se connecter au serveur WebSocket.
# En production, cela pourrait être wss://callpro.alex-com.tn
WS_URL=ws://localhost:3001